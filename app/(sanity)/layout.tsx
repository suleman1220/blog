export const metadata = {
  title: "Info Blog Admin",
  description: "Info Blog Admin"
};

export default function RootLayout({
  children
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
